import { Texture } from "pixi.js";
import { gAssetsPath } from "./globals";

export const offBtnTx = Texture.from(gAssetsPath + "pin_off.png");
export const onBtnTx = Texture.from(gAssetsPath + "pin_on.png");
